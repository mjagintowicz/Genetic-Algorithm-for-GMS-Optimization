**Requirements**
- Python environment (preferred 3.13)
- ```numpy```
- ```matplotlib```
- ```streamlit```

**App**

To run the app use:

```bash
streamlit run ui.py
```
In case the default port is occupied, try:
```bash
streamlit run ui.py --server.port free-port-number
```
